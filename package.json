{
  "name": "dalle-image-generator",
  "version": "1.0.0",
  "description": "MCP server for generating images using OpenAI's DALL-E 3",
  "main": "dist/mcp-server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/mcp-server.js",
    "dev": "tsc && node dist/mcp-server.js",
    "test": "tsc && node dist/test-mcp-server.js",
    "integration-test": "tsc && tsc -p tsconfig.integration.json && node dist/integration-test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.1",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "openai": "^4.95.1"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.3",
    "@types/express": "^5.0.1",
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3"
  }
}
